/**
 * Shared yellow CTA styling for homepage compare / hub actions (amber = kuning).
 */
export const homepageYellowCtaBase =
  "font-extrabold text-slate-900 shadow-lg transition hover:scale-[1.02] hover:bg-amber-500 active:scale-[0.98] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-amber-600";

export const homepageYellowCtaSolid = `inline-flex items-center justify-center gap-3 rounded-full bg-amber-400 ${homepageYellowCtaBase}`;
